import { StatefliterPipe } from './statefliter.pipe';

describe('StatefliterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatefliterPipe();
    expect(pipe).toBeTruthy();
  });
});
